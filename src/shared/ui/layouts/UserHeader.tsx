import React from 'react';
import HeaderTemplate from "@/shared/ui/layouts/HeaderTemplate";

const UserHeader = () => {
    return (
        <HeaderTemplate>
            <div className="w-100 px-3">me</div>
        </HeaderTemplate>
    );
};

export default UserHeader;
